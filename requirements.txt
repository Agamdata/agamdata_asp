fastapi==0.115.0
# python-multipart required by FastAPI's File/Form parsing
# (ASP-FEAT-ASP-04 v1.0 §6.1 multipart upload endpoint, I-DOC-05).
python-multipart==0.0.9
# pytesseract — OCR fallback for scanned-image PDFs (ASP-FEAT-ASP-04
# v1.0 §9.4 / I-DOC-07). Requires tesseract-ocr apt binary (in
# Dockerfile). Primary OCR path is pdftotext via pdfminer.six (for
# text-based PDFs) which is already installed; pytesseract only
# activates on empty-text fallback.
pytesseract==0.3.10
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2
redis[hiredis]==5.0.8
celery[redis]==5.4.0
flower==2.0.1
anthropic==0.34.2
langchain==0.3.0
langchain-anthropic==0.2.0
chromadb==0.5.7
sentence-transformers==3.0.1
boto3==1.35.0
httpx==0.27.2
structlog==24.4.0
python-dotenv==1.0.1
bcrypt==4.1.3
pdfminer.six==20231228
python-docx==1.1.2
slowapi==0.1.9
